как запустить

venv

pip install pytest


reduction of variables name (сокрашения в названиях переменных)
    comp = component
        component = it's a component of a ship. Example: weapon, hull and engine
    params
        parameters = it's characteristics of a component. Example: power (for engine)


